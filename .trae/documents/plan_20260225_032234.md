# 项目规则确立计划

根据您的澄清，我将仅执行以下操作以确立项目规则，而不修改任何工具代码：

## 1. 创建项目规则文档 (`PROJECT_RULES.md`)
在项目根目录创建此文件，明确写入以下两条铁律：
- **对话语言统一**：Agent 与用户的交互必须全程使用中文。
- **修改授权机制**：任何代码变更必须先提交计划，经用户明确同意后方可执行。

## 2. 核心记忆确认
我已将上述规则纳入核心记忆，确保在未来的交互中严格遵守。
